<div id="navigation">
    <div class="container-fluid">
        <%= link_to "CRM", :root, {:id => "brand"} %>
        <ul class='main-nav'>
            <% unless current_user.account_type == 4 %>
                <% permitted_to? :index, :leads do %>
                  <% if controller_name == "leads" %>
                      <li class='active'>
                        <% if current_user.account_type == 2 %>
                            <%= link_to("/leads/home/#{current_user.team_leader_id}") do %>
                                <span>Leads</span>
                            <% end %>
                        <% else %>
                            <%= link_to('/leads/home/') do %>
                                <span>Leads</span>
                            <% end %>
                        <% end %>
                      </li>
                  <% else%>
                      <li>
                        <% if current_user.account_type == 2 %>
                            <%= link_to("/leads/home/#{current_user.team_leader_id}") do %>
                                <span>Leads</span>
                            <% end %>
                        <% else %>
                            <%= link_to('leads/home') do %>
                                <span>Leads</span>
                            <% end %>
                        <% end %>
                      </li>
                  <% end %>

                  <% if controller_name == "accounts" %>
                      <% permitted_to? :index, :transactions do %>
                          <li class="active">
                            <a href="#" data-toggle="dropdown" class='dropdown-toggle'>
                              <span>Accounts</span>
                              <span class="caret"></span>
                            </a>
                            <ul class="dropdown-menu">
                              <li><a href='/accounts#/index?type=existing_accounts'>Existing accounts</a></li>
                              <li><a href='/accounts#/index?type=potential_accounts'>Potential Accounts</a></li>
                            </ul>
                          </li>
                      <% end %>
                  <% else %>
                      <% permitted_to? :index, :transactions do %>
                          <li>
                            <a href="#" data-toggle="dropdown" class='dropdown-toggle'>
                              <span>Accounts</span>
                              <span class="caret"></span>
                            </a>
                            <ul class="dropdown-menu">
                              <li><a href='/accounts#/index?type=existing_accounts'>Existing accounts</a></li>
                              <li><a href='/accounts#/index?type=potential_accounts'>Potential Accounts</a></li>
                            </ul>
                          </li>
                      <% end %>
                  <% end %>




                  <% permitted_to? :index, :transactions do %>
                      <% if controller_name == "products" || controller_name == "inventory_additions" %>
                          <li class="active">
                            <%= link_to "Products", :products %>
                          </li>
                      <% else %>
                          <li>
                            <%= link_to "Products", :products %>
                          </li>
                      <% end%>
                  <% end%>





                  <% if controller_name == "calendar" || controller_name == "events" || controller_name == "notifications" %>
                      <li class="active">
                        <a href="#" data-toggle="dropdown" class='dropdown-toggle'>
                          <span>Appointments</span>
                          <span class="caret"></span>
                        </a>
                        <ul class="dropdown-menu">
                          <li> <%= link_to "Calendar", :calendar %> </li>
                          <li> <%= link_to "Tasks", :events %> </li>
                          <li class="dropdown-submenu">
                            <a href="#" data-toggle="dropdown" class='dropdown-toggle'> <span>Notifications</span> </a>
                            <ul class="dropdown-menu d3-active">
                              <li> <%= link_to "Notification's list", {:controller=>"notifications", :action=>"index", :type=>"current"} %> </li>
                              <li> <%= link_to "Add new notifications", :new_notification %> </li>
                              <li> <%= link_to "Notifications to all", :notifications_to_all_notifications %> </li>
                            </ul>
                          </li>
                        </ul>
                      </li>
                  <% else %>
                      <li>
                        <a href="#" data-toggle="dropdown" class='dropdown-toggle'>
                          <span>Appointments</span>
                          <span class="caret"></span>
                        </a>
                        <ul class="dropdown-menu">
                          <li><%= link_to "Calendar", :calendar %></li>
                          <li> <%= link_to "Tasks", :events %> </li>
                          <li class="dropdown-submenu">
                            <a href="#" data-toggle="dropdown" class='dropdown-toggle'> <span>Notifications</span> </a>
                            <ul class="dropdown-menu d3">
                              <li> <%= link_to "Notification's list", {:controller=>"notifications", :action=>"index", :type=>"current"} %> </li>
                              <li> <%= link_to "Add new notifications", :new_notification %> </li>
                              <li> <%= link_to "Notifications to all", :notifications_to_all_notifications %> </li>
                            </ul>
                          </li>
                        </ul>
                      </li>
                  <% end %>


                    <% if controller_name == "users" %>
                        <% permitted_to? :index, :users do %>
                        <li class='active'>
                            <%= link_to(:users) do %>
                                <span>Users</span>
                            <% end %>
                        </li>
                        <% end %>
                    <% else %>
                        <% permitted_to? :index, :users do %>
                            <li>
                            <%= link_to(:users) do %>
                                <span>Users</span>
                            <% end %>
                            </li>
                        <% end %>
                    <% end %>

















                  <li class="active">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">Targets<b class="caret"></b></a>
                    <ul class="dropdown-menu">
                      <li><%= link_to "Targets", :targets %></li>
                    </ul>
                  </li>
                <% end %>
            <% end %>
        </ul>



      <div class="user">
        <ul class="icon-nav">
          <li class="dropdown sett">
            <a href="#" class='dropdown-toggle' data-toggle="dropdown" onclick="notification_fade_out()">
              <i class="icon-cog"></i>
              <span class="label lightred"><%= @lightred_counter %></span>
            </a>
            <ul class="dropdown-menu pull-right">
            </ul>
          </li>
        </ul>
        <div class="dropdown">
          <a href="#" class='dropdown-toggle' data-toggle="dropdown"><%= "#{current_user.first_name} #{current_user.last_name}" %><%= image_tag "#{current_user.avatar.url(:thumb)}" %></a>
          <ul class="dropdown-menu pull-right">
            <li>
              <%= link_to "Log out", destroy_user_session_path, :method => :delete %>
            </li>
          </ul>
        </div>
      </div>


    </div>
</div>
