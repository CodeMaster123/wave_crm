<%= form_for @partial_payment, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :amount_paid, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :amount_paid, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :transaction_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :transaction_id, :class => 'number_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                partial_payments_path, :class => 'btn' %>
  </div>
<% end %>
